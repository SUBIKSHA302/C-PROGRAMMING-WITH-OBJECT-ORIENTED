QUESTION:
Can we do in C++ with this header file.
#include<bits/stdc++.h>
 
intmain ()
{
   inta = 15, b = 20;
   printf("max = %d\n", ((a + b) + abs(a - b)) / 2);
   printf("min = %d", ((a + b) - abs(a - b)) / 2);
   return0;
}

ANSWER:
Yes,we can run the code with this header file
OUTPUT :
max = 20
min = 15
